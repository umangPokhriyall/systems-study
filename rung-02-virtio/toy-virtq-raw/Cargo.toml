[package]
name = "toy-virtq-raw"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "A split virtqueue implemented by hand, both halves, with no virtio crates."

[dependencies]
# None. A virtqueue is an agreement about the meaning of bytes in shared memory; implementing it
# needs nothing that is not in std. That is worth demonstrating rather than asserting.
